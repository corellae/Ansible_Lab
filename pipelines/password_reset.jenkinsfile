node{

  stage("Checkout SCM"){
    checkout scm
  }

  stage("Run Playbook"){
//    sh "ansible-playbook password_reset.yml -e 'ansible_ssh_user=ecorella target_user=${USER} new_password=${PASSWORD} servers=repo'"
    sh "whoami"
  }

}
