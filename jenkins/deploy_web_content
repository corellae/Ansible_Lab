node {
  def fileToDeploy = params.CONTENT
  
  stage('First Stage'){
    println "$fileToDeploy"
    sh "ls -l"
  }
  stage('checkout'){
    checkout scm
    sh "ls -l"
  }
  stage('test ansible'){
    sh "ansible -m debug repo"
  }
}
