node {
  def fileToDeploy = params.CONTENT
  
  stage('checkout'){
    checkout scm
  }
  stage('checkout'){
    checkout scm
    sh "ls -l"
  }
  stage('test ansible'){
    sh "ansible -m debug repo"
  }
}
