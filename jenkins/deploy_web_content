node {
  def fileToDeploy = params.CONTENT
  
  stage('checkout'){
    checkout scm
  }
  stage('listing'){
    sh "ls -l"
    sh "whoami"
  }
  stage('test ansible'){
    sh "ansible-playbook copy_web_files.yml -e 'ansible_ssh_user=ecorella { content : [${params.CONTENT}]}'"
  }
}
